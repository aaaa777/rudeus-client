<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="rudeus_client.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:rudeus_client"
    xmlns:view="clr-namespace:rudeus_client.View"
    >

    <FlyoutItem FlyoutDisplayOptions="AsMultipleItems">
        <Tab Title="Home"
             Icon="home.png">
            <ShellContent Title="Home1"
                          ContentTemplate="{DataTemplate local:MainPage}"
                          Route="MainPage" />

            <ShellContent Title="Home2"
                          ContentTemplate="{DataTemplate local:MainPage}"
                          Route="MainPage" />

            <ShellContent Title="Home3"
                          ContentTemplate="{DataTemplate local:MainPage}"
                          Route="MainPage" />
        </Tab>
    </FlyoutItem>
    <ShellContent
        Title="Home4"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />


    <ShellContent Title="Home5"
                  ContentTemplate="{DataTemplate local:MainPage}"
                  Route="MainPage" />

</Shell>
